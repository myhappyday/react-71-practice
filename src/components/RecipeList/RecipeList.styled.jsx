import { styled } from "styled-components";

export const List = styled.ul`
  display: flex;
  gap: 16px;
  list-style: none;
  padding: 0;
  margin: 0;
`;

export const ListItem = styled.ul`
  width: 240px;
`;